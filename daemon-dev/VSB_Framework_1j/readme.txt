The WEB-INF folder contains files that are required for using the VSB structure
and should be placed in same places in your webapp.

Read the VSB_Structure.txt files for details about how the VSB structuring is
done. The awk scripts are used to generate the form SCRIPTs and BEANs. The
VIEW is simple created by copying the template.